services:
  postgres:
    image: postgres:16
    container_name: life_manager_postgres
    restart: always
    environment:
      POSTGRES_DB: life_manager_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build:
      context: ./backend
      args:
        DATABASE_URL: postgresql://postgres:postgres@postgres:5432/life_manager_db?schema=public
    container_name: life_manager_backend
    restart: always
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/life_manager_db?schema=public
      JWT_SECRET: changeThisSecret
      FRONTEND_URL: http://localhost:5173
      PORT: 3000
    depends_on:
      - postgres

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE_URL: http://backend:3000/api/v1
    container_name: life_manager_frontend
    restart: always
    environment:
      VITE_API_BASE_URL: http://backend:3000/api/v1
    ports:
      - "5173:80"
    depends_on:
      - backend

volumes:
  postgres_data:
